# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Global configuration YAML with settings and hyperparameters for YOLO training, validation, prediction and export
# For documentation see https://docs.ultralytics.com/usage/cfg/

task: detect # (str) YOLO task, i.e. detect, segment, classify, pose, obb
mode: train # (str) YOLO mode, i.e. train, val, predict, export, track, benchmark

# Train settings -------------------------------------------------------------------------------------------------------
model: # (str, optional) path to model file, i.e. yolov8n.pt, yolov8n.yaml
data: # (str, optional) path to data file, i.e. coco8.yaml
epochs: 100 # (int) number of epochs to train for
time: # (float, optional) number of hours to train for, overrides epochs if supplied
patience: 100 # (int) epochs to wait for no observable improvement for early stopping of training
batch: 16 # (int) number of images per batch (-1 for AutoBatch)
imgsz: 640 # (int | list) input images size as int for train and val modes, or list[h,w] for predict and export modes
save: True # (bool) save train checkpoints and predict results
save_period: -1 # (int) Save checkpoint every x epochs (disabled if < 1)
cache: False # (bool) True/ram, disk or False. Use cache for data loading
device: # (int | str | list, optional) device to run on, i.e. cuda device=0 or device=0,1,2,3 or device=cpu
workers: 8 # (int) number of worker threads for data loading (per RANK if DDP)
project: # (str, optional) project name
name: # (str, optional) experiment name, results saved to 'project/name' directory
exist_ok: False # (bool) whether to overwrite existing experiment
pretrained: True # (bool | str) whether to use a pretrained model (bool) or a model to load weights from (str)
optimizer: auto # (str) optimizer to use, choices=[SGD, Adam, Adamax, AdamW, NAdam, RAdam, RMSProp, auto]
verbose: True # (bool) whether to print verbose output
seed: 0 # (int) random seed for reproducibility
deterministic: True # (bool) whether to enable deterministic mode
single_cls: False # (bool) train multi-class data as single-class
rect: False # (bool) rectangular training if mode='train' or rectangular validation if mode='val'
cos_lr: False # (bool) use cosine learning rate scheduler
close_mosaic: 10 # (int) disable mosaic augmentation for final epochs (0 to disable)
resume: False # (bool) resume training from last checkpoint
amp: True # (bool) Automatic Mixed Precision (AMP) training, choices=[True, False], True runs AMP check
fraction: 1.0 # (float) dataset fraction to train on (default is 1.0, all images in train set)
profile: False # (bool) profile ONNX and TensorRT speeds during training for loggers
freeze: None # (int | list, optional) freeze first n layers, or freeze list of layer indices during training
multi_scale: False # (bool) Whether to use multiscale during training
# Segmentation
overlap_mask: True # (bool) merge object masks into a single image mask during training (segment train only)
mask_ratio: 4 # (int) mask downsample ratio (segment train only)
# Classification
dropout: 0.0 # (float) use dropout regularization (classify train only)

# Val/Test settings ----------------------------------------------------------------------------------------------------
val: True # (bool) validate/test during training
split: val # (str) dataset split to use for validation, i.e. 'val', 'test' or 'train'
save_json: False # (bool) save results to JSON file
save_hybrid: False # (bool) save hybrid version of labels (labels + additional predictions)
conf: # (float, optional) object confidence threshold for detection (default 0.25 predict, 0.001 val)
iou: 0.7 # (float) intersection over union (IoU) threshold for NMS
max_det: 300 # (int) maximum number of detections per image
half: False # (bool) use half precision (FP16)
dnn: False # (bool) use OpenCV DNN for ONNX inference
plots: True # (bool) save plots and images during train/val

# Predict settings -----------------------------------------------------------------------------------------------------
source: # (str, optional) source directory for images or videos
vid_stride: 1 # (int) video frame-rate stride
stream_buffer: False # (bool) buffer all streaming frames (True) or return the most recent frame (False)
visualize: False # (bool) visualize model features
augment: False # (bool) apply image augmentation to prediction sources
agnostic_nms: False # (bool) class-agnostic NMS
classes: # (int | list[int], optional) filter results by class, i.e. classes=0, or classes=[0,2,3]
retina_masks: False # (bool) use high-resolution segmentation masks
embed: # (list[int], optional) return feature vectors/embeddings from given layers

# Visualize settings ---------------------------------------------------------------------------------------------------
show: False # (bool) show predicted images and videos if environment allows
save_frames: False # (bool) save predicted individual video frames
save_txt: False # (bool) save results as .txt file
save_conf: False # (bool) save results with confidence scores
save_crop: False # (bool) save cropped images with results
show_labels: True # (bool) show prediction labels, i.e. 'person'
show_conf: True # (bool) show prediction confidence, i.e. '0.99'
show_boxes: True # (bool) show prediction boxes
line_width: # (int, optional) line width of the bounding boxes. Scaled to image size if None.

# Export settings ------------------------------------------------------------------------------------------------------
format: torchscript # (str) format to export to, choices at https://docs.ultralytics.com/modes/export/#export-formats
keras: False # (bool) use Kera=s
optimize: False # (bool) TorchScript: optimize for mobile
int8: False # (bool) CoreML/TF INT8 quantization
dynamic: False # (bool) ONNX/TF/TensorRT: dynamic axes
simplify: True # (bool) ONNX: simplify model using `onnxslim`
opset: # (int, optional) ONNX: opset version
workspace: None # (float, optional) TensorRT: workspace size (GiB), `None` will let TensorRT auto-allocate memory
nms: False # (bool) CoreML: add NMS

# Hyperparameters ------------------------------------------------------------------------------------------------------
lr0: 0.01 # (float) initial learning rate (i.e. SGD=1E-2, Adam=1E-3)
lrf: 0.01 # (float) final learning rate (lr0 * lrf)
momentum: 0.937 # (float) SGD momentum/Adam beta1
weight_decay: 0.0005 # (float) optimizer weight decay 5e-4
warmup_epochs: 3.0 # (float) warmup epochs (fractions ok)
warmup_momentum: 0.8 # (float) warmup initial momentum
warmup_bias_lr: 0.0 # 0.1 # (float) warmup initial bias lr
box: 7.5 # (float) box loss gain
cls: 0.5 # (float) cls loss gain (scale with pixels)
dfl: 1.5 # (float) dfl loss gain
pose: 12.0 # (float) pose loss gain
kobj: 1.0 # (float) keypoint obj loss gain
nbs: 64 # (int) nominal batch size
hsv_h: 0.015 # (float) image HSV-Hue augmentation (fraction)
hsv_s: 0.7 # (float) image HSV-Saturation augmentation (fraction)
hsv_v: 0.4 # (float) image HSV-Value augmentation (fraction)
degrees: 0.0 # (float) image rotation (+/- deg)
translate: 0.1 # (float) image translation (+/- fraction)
scale: 0.5 # (float) image scale (+/- gain)
shear: 0.0 # (float) image shear (+/- deg)
perspective: 0.0 # (float) image perspective (+/- fraction), range 0-0.001
flipud: 0.0 # (float) image flip up-down (probability)
fliplr: 0.5 # (float) image flip left-right (probability)
bgr: 0.0 # (float) image channel BGR (probability)

mosaic: 1.0 # (float) image mosaic (probability)
mixup: 0.0 # (float) image mixup (probability)
copy_paste: 0.1 # (float) segment copy-paste (probability)

copy_paste_mode: "flip" # (str) the method to do copy_paste augmentation (flip, mixup)
auto_augment: randaugment # (str) auto augmentation policy for classification (randaugment, autoaugment, augmix)
erasing: 0.4 # (float) probability of random erasing during classification training (0-0.9), 0 means no erasing, must be less than 1.0.
crop_fraction: 1.0 # (float) image crop fraction for classification (0.1-1), 1.0 means no crop, must be greater than 0.

# Custom config.yaml ---------------------------------------------------------------------------------------------------
cfg: # (str, optional) for overriding defaults.yaml

# Tracker settings ------------------------------------------------------------------------------------------------------
tracker: botsort.yaml # (str) tracker type, choices=[botsort.yaml, bytetrack.yaml]


# =============================================================================
# ABLATION STUDY PARAMETERS — DEDUPLICATED
# One occurrence per key. ALL defaults are the INERT / anchor values: a run
# only activates a mechanism by passing the parameter to model.train(...).
# (YAML silently keeps the LAST occurrence of a duplicated key, so the old
# file's earlier definitions of area_weight_mode / iou_type / use_nwd /
# vfl_alpha / vfl_gamma were dead text — the values kept below are the ones
# that were actually in effect, except where marked CHANGED.)
# =============================================================================

# --- Section A: size-aware weighting (current loss.py, NEW-1/NEW-2 aware) ----
alpha_start: 0.0          # CHANGED 0.7 -> 0.0: inert default (Section A off)
alpha_end: 0.0            # CHANGED 0.4 -> 0.0
alpha_min: 0.0            # CHANGED 0.3 -> 0.0
alpha_max: 0.0            # CHANGED 0.7 -> 0.0
small_obj_px: 48          # inert while boost=1 / dfl_small_boost=1 / nwd_ratio=0
small_obj_boost: 1.0      # 1.0 = off
weight_renorm: 1          # NEW-1: renormalize combined weight (identity at alpha=0)
area_mode: fixed          # NEW-2: 'fixed' | 'legacy'
area_ref_px: 64.0         # NEW-2: reference box side (px)
area_gamma: 0.5           # NEW-2: 0.5 = sqrt weighting, 1.0 = inverse
area_w_cap: 3.0           # NEW-2: max area weight

# --- Section A (legacy keys, older loss versions only) -----------------------
area_weight_min: 0.4        # legacy floor (unused by current loss.py)
area_weight_mode: inv       # was duplicated ('log_inverse' then 'inv'); 'inv' was in effect
small_obj_boost_backpack: -1.0  # <=0 disables; falls back to small_obj_boost
small_obj_boost_bag: -1.0
small_obj_boost_trolley: -1.0

# --- Section A' / A'': targeted small-object terms (NEW-3 / NEW-5) -----------
dfl_small_boost: 1.0      # NEW-3: 1.0 = off
nwd_ratio: 0.0            # NEW-5: 0.0 = off
nwd_c: 64.0               # NEW-5: NWD temperature (px). NOTE: distinct from
                          # legacy 'nwd_C' below (case-sensitive!) — different
                          # mechanism, different loss version.

# --- Section B: center loss --------------------------------------------------
center_loss_weight_init: 0.0   # 0 = off
center_loss_weight_min: 0.0
center_loss_decay_epochs: 35
center_loss_mode: small        # legacy B2: 'small' | 'crowd'
center_crowd_iou: 0.1

# --- Section C: adaptive clipping --------------------------------------------
iou_clip_start: 1000.0    # effectively disabled
iou_clip_end: 1000.0
dfl_clip_start: 1000.0
dfl_clip_end: 1000.0
use_percentile_clip: False
clip_percentile: 0.95
use_loss_clip: False      # legacy Round-4 toggle

# --- Section D: TAL assignment (stock defaults) ------------------------------
tal_topk: 10
tal_alpha: 0.5
tal_beta: 6.0

# --- Section D variants: SA-TAL / shape-TAL / AR-TAL (all off) ---------------
use_satal: False
satal_alpha_small: 1.2
satal_beta_small: 4.5
satal_alpha_large: 1.0
satal_beta_large: 6.0
satal_small_area: 0.0025
satal_large_area: 0.0225
satal_topk_factor: 1.3
use_shape_tal: False
shape_gamma: 0.1
shape_min: 0.1
use_artal: False          # Section M
artal_ar_thresh: 2.0
artal_ar_scale: 2.0
artal_beta_relax: 2.0

# --- Section E: classification loss (NEW-4 + legacy toggles) -----------------
cls_loss: bce             # NEW-4: 'bce' | 'vfl' (current loss.py)
vfl_alpha: 0.75           # was duplicated (once with trailing commas = string bug); fixed
vfl_gamma: 2.0
use_vfl: False            # CHANGED "True," (string, bug) -> False; legacy key,
                          # older loss versions only — current file uses cls_loss
cls_mode: bce             # legacy Round-4: 'bce' | 'qfl'
qfl_beta: 2.0
use_cls_swa: False        # Section K
cls_swa_boost: 1.75

# --- Section E2: class balancing (legacy) ------------------------------------
use_class_weighting: False   # CHANGED True -> False: inert default. Activate
                             # per-run; a silently-on class weighting would
                             # contaminate every anchor.
class_weight_mode: sqrt      # 'sqrt' | 'linear'
class_balance_strength: 0.0  # CHANGED 1.0 -> 0.0: inert default
class_frequencies:
  - 0.267    # backpack
  - 0.219    # bag
  - 0.513    # trolley
class_weights: null          # Section H alt: e.g. '1.05,1.15,0.80', null = off
use_bag_penalty: False       # Section L
bag_penalty_weight: 2.0
bag_class_id: 1              # backpack=0, bag=1, trolley=2

# --- Sections F/H/I: legacy regression variants (older loss versions) --------
iou_type: ciou            # was duplicated ("DIoU" then ciou); ciou was in effect
box_loss_type: ciou       # Round-4: ciou | mpdiou | wiou | focaler
wiou_alpha: 1.9
wiou_delta: 3.0
wiou_momentum: 0.02
focaler_d: 0.0
focaler_u: 0.95
use_nwd: False            # was duplicated (True then False); False was in effect
nwd_mode: small_only      # Round-4 NWD: pure | blend | small_only
nwd_weight: 0.5
nwd_C: 4.0                # Round-4 NWD constant (distinct from NEW-5 nwd_c!)
nwd_small_threshold: 32.0
nwd_gate_px: 48.0         # Section F size-gated NWD
nwd_const: 12.8
use_repulsion: False      # Section J
repulsion_weight: 0.3

# --- Section G: aspect-ratio importance weighting ----------------------------
ar_lambda: 0.0            # 0 = off
ar_ref: 1.5
ar_cap: 2.0

# --- Legacy SWA extras (Round-4) ---------------------------------------------
swa_smooth: False
swa_boost_power: 0.5
iarw_gamma: 0.0
dfl_iou_gated: 0
nwd_adaptive: 0
nwd_anneal: 0
nwd_anneal_min: 0.1

# --- Round 10 v1: regression-signal mechanisms ---
alpha_iou: 1.0            # NEW-10  power-IoU exponent (1.0 = off)
l1_aux_weight: 0.0        # NEW-11  Smooth/Balanced-L1 pixel-residual weight (0 = off)
l1_aux_beta: 2.0          # NEW-11  L1 transition point, pixels
l1_aux_small_only: 1      # NEW-11  1 = small objects only
l1_balanced: 0            # NEW-11  0 = Smooth-L1, 1 = Balanced-L1
l1_balanced_alpha: 0.5    # NEW-11  Balanced-L1 alpha
l1_balanced_gamma: 1.5    # NEW-11  Balanced-L1 gamma
dfl_entropy_weight: 0.0   # NEW-12  DFL distribution entropy penalty (0 = off)
dfl_entropy_small_only: 1 # NEW-12  1 = small objects only
nwd_c_adaptive: 0         # NEW-13  1 = size-adaptive NWD temperature
nwd_c_k: 0.5              # NEW-13  adaptive-c scale factor
tightness_gamma: 0.0      # NEW-14  asymmetric spill-over penalty (0 = off)
tightness_small_only: 1   # NEW-14  1 = small objects only

box_metric: ciou
rel_l1_weight: 0.0
rel_l1_small_only: 1
tal_nwd: 0
tal_nwd_c: 8.0
tal_nwd_area: 2304.0
tal_nwd_ratio: 1.0
