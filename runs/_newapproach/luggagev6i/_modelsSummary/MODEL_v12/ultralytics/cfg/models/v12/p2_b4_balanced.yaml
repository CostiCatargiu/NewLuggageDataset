# VARIANT B4: P2 Auxiliary + Balanced
# Change: Reduce P5, boost aux branches

nc: 4
scales:
  n: [0.50, 0.25, 1024]

backbone:
  - [-1, 1, Conv, [64, 3, 2]]           # 0
  - [-1, 1, Conv, [128, 3, 2, 1, 2]]    # 1
  - [-1, 3, C3k2, [256, False, 0.25]]   # 2 ← CHANGED: 2->3
  - [-1, 1, Conv, [256, 3, 2, 1, 4]]    # 3
  - [-1, 4, C3k2, [512, False, 0.25]]   # 4 ← CHANGED: 3->4
  - [-1, 1, Conv, [512, 3, 2]]          # 5
  - [-1, 4, A2C2f, [512, True, 4]]      # 6
  - [-1, 1, Conv, [1024, 3, 2]]         # 7
  - [-1, 3, A2C2f, [1024, True, 1]]     # 8 ← CHANGED: 5->3 (reduced)

head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]]
  - [-1, 2, A2C2f, [512, False, -1]]    # 11

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]]
  - [-1, 3, A2C2f, [256, False, -1]]    # 14 ← CHANGED: 2->3

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 2], 1, Concat, [1]]
  - [-1, 3, C3k2, [128, False, 0.25]]   # 17 ← CHANGED: 2->3

  # Auxiliary - STRONGER
  - [-1, 2, C3k2, [128, True]]          # 18 ← CHANGED: 1->2

  # Standard P2
  - [17, 1, Conv, [128, 3, 1]]          # 19

  # Bottom-up path
  - [-1, 1, Conv, [128, 3, 2]]
  - [[-1, 14], 1, Concat, [1]]
  - [-1, 3, C3k2, [256, False, 0.25]]   # 22 ← CHANGED: 2->3

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 11], 1, Concat, [1]]
  - [-1, 2, A2C2f, [512, False, -1]]    # 25

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 8], 1, Concat, [1]]
  - [-1, 2, C3k2, [1024, True]]         # 28 ← CHANGED: 3->2 (reduced)

  - [[18, 19, 22, 25, 28], 1, Detect, [nc]]
